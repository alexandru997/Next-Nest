# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthPayload {
  email: String!
  name: String!
  role: String!
  token: String!
}

type Company {
  country: String
  createdAt: DateTime!
  id: ID!
  industry: String
  name: String!
  updatedAt: DateTime!
}

input CompanyInput {
  country: String
  industry: String
  name: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type EmissionReport {
  co2Emissions: Float
  companyId: String!
  createdAt: DateTime!
  createdById: String!
  energyUsage: Float
  id: ID!
  notes: String
  quarter: Int
  renewablePct: Float
  wasteGenerated: Float
  waterUsage: Float
  year: Int!
}

input GoalInput {
  baselineValue: Float!
  baselineYear: Int!
  companyId: String!
  metric: MetricType!
  targetValue: Float!
  targetYear: Int!
}

input LoginInput {
  email: String!
  password: String!
}

enum MetricType {
  CO2
  ENERGY
  WASTE
  WATER
}

type Mutation {
  createCompany(input: CompanyInput!): Company!
  createGoal(input: GoalInput!): ReductionGoal!
  createReport(input: ReportInput!): EmissionReport!
  deleteCompany(id: ID!): Boolean!
  deleteGoal(id: ID!): Boolean!
  deleteReport(id: ID!): Boolean!
  login(input: LoginInput!): AuthPayload!
  register(input: RegisterInput!): AuthPayload!
  updateCompany(id: ID!, input: CompanyInput!): Company!
  updateReport(id: ID!, input: ReportInput!): EmissionReport!
}

type Query {
  companies: [Company!]!
  company(id: ID!): Company!
  goals(companyId: ID!): [ReductionGoal!]!
  me: AuthPayload!
  report(id: ID!): EmissionReport!
  reports(companyId: ID!, year: Int): [EmissionReport!]!
}

type ReductionGoal {
  baselineValue: Float!
  baselineYear: Int!
  companyId: String!
  createdAt: DateTime!
  id: ID!
  metric: MetricType!
  targetValue: Float!
  targetYear: Int!
}

input RegisterInput {
  email: String!
  name: String!
  password: String!
}

input ReportInput {
  co2Emissions: Float
  companyId: String!
  energyUsage: Float
  notes: String
  quarter: Int
  renewablePct: Float
  wasteGenerated: Float
  waterUsage: Float
  year: Int!
}